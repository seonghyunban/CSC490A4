\subsubsection{Ablation Design}
\nestindent

We conduct ten runs, each varying only the reward function composition while holding all other variables constant.
Every run includes the binary correctness reward; the C+D run specifically tests whether non-degeneracy is a prerequisite for entity grounding to be effective.

\input{component/figures/p4/t5_ablation}
\nestunindent
