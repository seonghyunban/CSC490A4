Table~\ref{tab:p4-summary} presents headline metrics across all pre-P3 runs.

\input{component/figures/p4/t1_summary_pre}

Reward A (format compliance) produces the strongest individual improvement, raising Pass@1 from \pFourBaselinePassOne\% to \pFourSeparateAPassOne\% (\pFourSeparateADeltaPassOne{}pp) and cutting extraction failures from \pFourBaselineExtrFail\% to \pFourSeparateAExtrFail\%.
Reward B (numeric proximity) provides a smaller gain of \pFourSeparateBDeltaPassOne{}pp, below the 1\% noise threshold established in our significance criteria.
The A+B run achieves \pFourCombinedAbDeltaPassOne{}pp, slightly below A alone (\pFourSeparateADeltaPassOne{}pp), suggesting mild interference when rewards are combined.

Table~\ref{tab:p4-gained-lost} decomposes accuracy changes into per-problem gains and losses relative to the baseline.

\input{component/figures/p4/t2_gained_lost_pre}

All runs show significant churn: even the best run (+A) loses \pFourSeparateALost{} problems while gaining \pFourSeparateAGained.
This indicates that reward additions do not monotonically improve---they shift which problems the model solves, not just how many.
Reward B shows particularly high churn relative to its net gain (\pFourSeparateBGained{} gained, \pFourSeparateBLost{} lost, net \pFourSeparateBNet), suggesting it redistributes capability rather than adding it.
