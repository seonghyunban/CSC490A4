Table~\ref{tab:p4-errors-full} extends the error distribution to all ten runs.

\input{component/figures/p4/t4_errors_post}

Number grounding (E) produces the most extreme format-failure reduction of any individual reward: ``no answer'' drops to \pFourSeparateENoAnswer\% ($-$21.2pp from baseline) and ``format only'' to \pFourSeparateEFormatOnly\% ($-$30.0pp).
This surpasses even non-degeneracy (C), suggesting that number grounding's signal---rewarding responses that use the question's numbers---implicitly encourages structured mathematical responses that include parseable answers.

The C+D+E combination achieves the lowest format-failure rates of any configuration: \pFourCombinedCdeNoAnswer\% ``no answer'' and \pFourCombinedCdeFormatOnly\% ``format only'', with zero gibberish.
Its dominant failure mode is ``large error'' at \pFourCombinedCdeLargeError\%, the highest of any configuration, indicating that the grounding reward stack nearly eliminates surface-level failures, leaving only fundamental reasoning errors.
